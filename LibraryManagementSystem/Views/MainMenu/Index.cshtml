@using LibraryManagementSystem.Models

@{
    ViewData["Title"] = "Index";
}

@model UserModel

<!DOCTYPE html>
<html>
<head>
    <link rel="stylesheet" type="text/css" href="~/_Layout.cshtml.css">
    <style>
        .custom-element {
            background-color: #007bff;
            color: #fff;
            padding: 10px;
        }
        .container {
            display: flex;
            justify-content: center;
            align-items: flex-start; /* Align items at the top of the container */
            height: 100vh; /* Set the height to the viewport height */
        }

        /* Style for the centered elements */
        .centered-elements {
            text-align: center; /* Center the content horizontally */
            margin-top: 20px; /* Add top margin if needed */
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="centered-elements">
            <h2>Main Menu</h2>
    
            <p>Welcome, @Model.Username!</p>
          
            <p>Your Loyalty Points: @Model.LoyaltyPoints</p>
    
            @if (Model.Role == UserModel.UserRole.admin)
            {
                <p>You have administrative privileges.</p>
            }
                else if (Model.Role == UserModel.UserRole.librarian)
            {
                <p>You are a librarian.</p>
            }
            else if (Model.Role == UserModel.UserRole.patron)
            {
                <p>You are a patron.</p>
            }

            <form asp-controller="Payment" asp-action="Index" asp-route-userId="@Model.UserId" method="post">
                <button type="submit">Pay late fees.</button>
            </form>

            <form asp-controller="Library" asp-action="Index" asp-route-userId="@Model.UserId" method="post">
                <button type="submit">Library</button>
            </form>        
        </div>
    </div>
</body>
</html>
